server:
  port: 8082
spring:
  kafka:
    streams:
      bootstrap-servers: localhost:9092
      properties:
        spring:
          json:
            trusted:
              packages: "*"
            value:
              default:
                type: java.lang.String
        state.dir: ./kafka-streams
    consumer:
      group-id: game-data

  cloud:
    stream:
      kafka:
        streams:
          binder:
            brokers: localhost:9092,localhost:9093
            application-id: game-data
            configuration:
              default:
                key:
                  serde: org.apache.kafka.common.serialization.Serdes$StringSerde
                value:
                  serde: io.confluent.kafka.streams.serdes.avro.GenericAvroSerde
                deserialization.exception.handler: by.bsuir.seabattle.exception.DeserializationExceptionHandlerImpl
              specific.avro.reader: true
              schema.registry.url: http://localhost:8081
              dynamicSchemaGenerationEnabled: false
      bindings:
        processGameCreationRequests-in-0:
          destination: game-creation-requests
        processGameCreationRequests-out-0:
          destination: games-topic
      instance-count: 2
      instance-index: 0

    function:
      definition: processGameCreationRequests;
  main:
    allow-bean-definition-overriding: true